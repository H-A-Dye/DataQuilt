name: data_quilt
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - setuptools
  - codecov
  - pip
  - requests
  - pillow
  - pandas
  - jupyter
  - ipython
  - pylint
  - pytest>=6.0
  - pytest-cov
  - geopy
  - pip:
      - matplotlib
      - swatch
    
variables:
  TOKEN: ***REMOVED***

